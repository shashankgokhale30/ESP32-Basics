# ESP32 Touch Pin

This project uses the ESP32's built-in touch sensor to control the onboard LED.

## Components
- ESP32 Dev Board

## What it does
When the touch pin is touched, the LED turns ON. Otherwise, it stays OFF.

## What I learned
- touchRead()
- if-else statements
- Touch sensor basics
